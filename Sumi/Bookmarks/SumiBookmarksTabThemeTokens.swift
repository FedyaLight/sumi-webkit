import SwiftUI

enum SumiBookmarksTabTypography {
    static let sidebarTitle = Font.system(size: 28, weight: .semibold)
    static let moreActionsIcon = Font.system(size: 15, weight: .semibold)
    static let emptyStateIcon = Font.system(size: 42)
    static let rowTitle = Font.system(size: 14, weight: .medium)
    static let folderIcon = Font.system(size: 18, weight: .medium)
    static let bookmarkFallbackIcon = Font.system(size: 17, weight: .medium)
}

enum SumiBookmarksTabColors {
    static let transparent = Color.clear
}
