import AppKit
import SwiftUI

enum SidebarInsertionGuide {
    /// The SpaceView owns the 4pt gap below SpaceTitle; pinned rows start
    /// directly after that gap.
    static let visualCenterY: CGFloat = 0
}
